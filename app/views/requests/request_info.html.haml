/ Requests show to administrator
%h1 Listing requests

%table
  %tr
    %th Name
    %th Email
    %th Phone Number
    %th Request type
    %th Status


  - @requests.each do |request|
    - if request.status == "unread"
      %tr{:bgcolor => "FFFFCC"}
        %td= request.user.name
        %td= request.user.id
        %td= request.phone_no
        %td= link_to request.request_type, user_request_path(params[:user_id],request.id) 
        %td= request.status
    - else
      %tr
        %td= request.user.name
        %td= request.user.id
        %td= request.phone_no
        %td= link_to request.request_type, user_request_path(params[:user_id],request.id) 
        %td= request.status

%br

